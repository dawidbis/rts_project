// main.cpp - rts_client entry point (skeleton)

#include <spdlog/spdlog.h>

int main(int /*argc*/, char* /*argv*/[]) {
    spdlog::info("rts-client starting (skeleton build)");
    // TODO: create SFML window
    // TODO: initialize ImGui-SFML
    // TODO: connect to server via ENet
    // TODO: main render/input loop
    spdlog::info("rts-client shutting down");
    return 0;
}
