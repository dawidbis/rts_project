// placeholder.cpp
// Temporary translation unit to satisfy CMake's requirement that a static
// library has at least one source file. Remove once real sources are added.

namespace rts::common {
[[maybe_unused]] inline int placeholder() noexcept { return 0; }
}  // namespace rts::common
